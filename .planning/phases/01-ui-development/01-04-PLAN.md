---
phase: 01-ui-development
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - components/DownloadView.tsx
  - app/download/page.tsx
  - __tests__/DownloadView.test.tsx
autonomous: true
requirements: [UI-DOWNLOAD]
tags: [react, post-session, download]

must_haves:
  truths:
    - "/download renders the .post-wrap layout with .post-head ('Your sessions.', '4 takes · 2h 11m total', + New session button) and a .rec-list of session rows"
    - "Each session row shows the title (with italic accent on title? — match HTML), guest + date inline, duration cell, size cell, and a Download action"
    - "Clicking + New session navigates to /lobby"
    - "Component accepts an optional sessions prop and falls back to the four placeholder rows from the HTML prototype"
  artifacts:
    - path: "components/DownloadView.tsx"
      provides: "Post-session list with placeholder data + New Session button"
      contains: "Your"
    - path: "app/download/page.tsx"
      provides: "Download route renders <DownloadView />"
      contains: "DownloadView"
  key_links:
    - from: "components/DownloadView.tsx"
      to: "/lobby"
      via: "useRouter().push on New session click"
      pattern: "router\\.push\\(['\"]/lobby['\"]\\)"
---

## Phase Goal

**As a** podcast host, **I want to** see a list of my recorded sessions with file sizes and a download link, **so that** I can retrieve the mixed audio file after each session.

<objective>
Build the post-session view (`/download`) as the fourth and final vertical slice of Phase 1. The view renders placeholder session data matching the HTML prototype; Phase 4 will pass real session data via the `sessions` prop.

Purpose: Completes the full Phase 1 UI surface. After this plan, every route shown in the HTML prototype has a fully rendered React equivalent. This plan runs in parallel with Plan 03 (different files, no overlap).
Output: A working `/download` route showing four placeholder recording rows, a header, and a New Session action that returns the user to the lobby.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-development/01-CONTEXT.md
@.planning/phases/01-ui-development/01-RESEARCH.md
@.planning/phases/01-ui-development/01-PATTERNS.md
@.planning/phases/01-ui-development/01-02-SUMMARY.md
@IC Podcast Recorder.html

<interfaces>
Props (from PATTERNS.md):
```
interface Session {
  id: string
  title: string
  guest: string
  date: string
  duration: string
  sizeMB: string
}
interface DownloadViewProps {
  sessions?: Session[]
  onNewSession?: () => void
  onDownload?: (sessionId: string) => void
}
```

Markup (HTML lines 629-666 — render verbatim by class name):
- `.post-wrap`
  - `.post-head`
    - left: `<div class="ttl">Your <i>sessions.</i></div>` and `<div class="sub">4 takes · 2h 11m total</div>`
    - right: `<Button variant="primary">+ New session</Button>` (the HTML uses `+ &nbsp;New session`; render as `+ New session` with appropriate spacing)
  - `.rec-list` containing one `.rec-row` per session:
    - `<div class="ttle">{title}<span class="ep">with {guest} · {date}</span></div>`
    - `<div class="cell hide-sm"><b>{duration}</b></div>`
    - `<div class="cell hide-sm">{sizeMB}</div>`
    - `<div class="dl">↓ Download</div>` (clickable; calls onDownload(session.id) — Phase 1 just no-ops)

Placeholder defaults (from HTML lines 640-663):
1. id 's1', title 'Orbit Drift, Ep. 17', guest 'Mira Senna', date 'May 21', duration '01:08:42', sizeMB '62.3 MB'
2. id 's2', title 'Late Signal, Ep. 04', guest 'Jamie Solas', date 'May 14', duration '00:47:11', sizeMB '41.7 MB'
3. id 's3', title 'Backchannel, Ep. 02', guest 'Owen Pell', date 'May 07', duration '00:32:08', sizeMB '28.4 MB'
4. id 's4', title 'Field Notes, Ep. 11', guest 'Rae Quill', date 'Apr 30', duration '00:23:14', sizeMB '20.1 MB'

(If the HTML uses different titles/values, prefer the HTML values verbatim — the list above is from PATTERNS.md and may need a one-line correction after reading the file.)

Component is a Client Component (`'use client'`) only because of the `useRouter` call for the default `onNewSession` handler. If `onNewSession` is always passed in, the component could be a Server Component; for Phase 1 keep it client-side for consistency with other view components.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement DownloadView with placeholder data</name>
  <files>components/DownloadView.tsx, __tests__/DownloadView.test.tsx</files>
  <read_first>
    - .planning/phases/01-ui-development/01-PATTERNS.md (`components/DownloadView.tsx` section, Shared Patterns)
    - IC Podcast Recorder.html (lines 629-666 — copy actual session titles/values verbatim)
    - components/ui/Button.tsx
    - app/globals.css (verify .post-wrap, .post-head, .ttl, .sub, .rec-list, .rec-row, .cell, .hide-sm, .dl rules exist from Plan 01)
  </read_first>
  <behavior>
    - Component is a Client Component (`'use client'`).
    - Renders the `.post-wrap` markup tree verbatim by class name as described in the interfaces block.
    - Header `.ttl` uses `<i>sessions.</i>` for the italic accent (existing CSS rule colors it).
    - The `Sub` text is hard-coded to match the HTML (`4 takes · 2h 11m total`) when no sessions prop is provided; when a `sessions` prop is provided, render `${sessions.length} takes` with no total duration (Phase 4 can compute and pass a richer sub-line).
    - The `+ New session` button uses `Button` primitive with `variant="primary"`. On click, calls `onNewSession` if provided, else falls back to `useRouter().push('/lobby')`.
    - Each session row renders the four `.ttle` / `.cell hide-sm` / `.cell hide-sm` / `.dl` divs in that order. The `.dl` element is a `<button>` styled as a link (or a `<div role="button" tabIndex={0}>`) so it is keyboard accessible. Clicking it calls `onDownload?.(session.id)` — no default behavior in Phase 1.
    - When `sessions` prop is undefined or empty, fall back to a hard-coded array of four sessions whose values are read directly from the HTML prototype (lines 640-663). DO NOT invent titles or numbers — copy them.
    - Test (`__tests__/DownloadView.test.tsx`): mocks `next/navigation` (`useRouter` returning `{ push: pushMock }`), renders `<DownloadView />`, asserts the headline contains `Your` and `sessions.`, asserts at least four `.rec-row` rows render (use `container.querySelectorAll('.rec-row').length >= 4`), asserts the first row's title text matches the first placeholder from the HTML, clicks the `+ New session` button and asserts `pushMock` was called with `/lobby`. Also asserts clicking a `.dl` element does not throw when no `onDownload` is provided.
  </behavior>
  <action>
    Implement `components/DownloadView.tsx` per PATTERNS.md. Open `IC Podcast Recorder.html`, navigate to lines 629-666, and copy the four session row values (title, guest, date, duration, size) into a `DEFAULT_SESSIONS` constant inside `DownloadView.tsx`. Use the exact strings from the HTML — do not paraphrase. Render the structure exactly as the HTML provides it, swapping the static `<button>` and the static `.dl` divs for accessible alternatives where useful. Use the `Button` primitive for the `+ New session` action with `variant="primary"`.

    Write `__tests__/DownloadView.test.tsx` covering the assertions above using `@testing-library/react` and `user-event`. Use `container.querySelectorAll('.rec-row')` to count rows (text matching for titles is brittle if values change; using class selectors is acceptable per RTL guidance for structural assertions tied to global CSS).
  </action>
  <verify>
    <automated>cd /Users/codysecret1/Desktop/IC-podcast && grep -q "'use client'" components/DownloadView.tsx && grep -q "post-wrap" components/DownloadView.tsx && grep -q "rec-row" components/DownloadView.tsx && grep -q "DEFAULT_SESSIONS\\|defaultSessions" components/DownloadView.tsx && grep -q "router.push('/lobby')" components/DownloadView.tsx && npm run build && npm run test:ci -- DownloadView</automated>
  </verify>
  <acceptance_criteria>
    - First non-empty line of `components/DownloadView.tsx` is `'use client'`.
    - `components/DownloadView.tsx` contains the literal strings `post-wrap`, `post-head`, `rec-list`, `rec-row`, `ttle`, `cell hide-sm`, `dl`, and `<i>sessions.</i>` (or its React equivalent).
    - `components/DownloadView.tsx` contains the literal `router.push('/lobby')`.
    - `components/DownloadView.tsx` contains a fallback session array with at least four entries whose values match the HTML prototype rows 640-663.
    - `__tests__/DownloadView.test.tsx` passes.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>DownloadView renders the post-session layout with four placeholder rows; New Session navigates back to /lobby; tests cover structural assertions and the navigation handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire /download route to DownloadView</name>
  <files>app/download/page.tsx</files>
  <read_first>
    - app/download/page.tsx (existing placeholder from Plan 01)
    - components/DownloadView.tsx (from Task 1)
    - .planning/phases/01-ui-development/01-PATTERNS.md (thin shell pattern)
  </read_first>
  <behavior>
    - `app/download/page.tsx` is a Server Component (no `'use client'`).
    - It imports `DownloadView` from `@/components/DownloadView` and returns `<DownloadView />`.
    - The Plan 01 placeholder comment and `<h1>Download</h1>` content are removed.
  </behavior>
  <action>
    Replace `app/download/page.tsx` with a thin Server-Component shell that renders `<DownloadView />`, matching the pattern used by `app/login/page.tsx` and `app/lobby/page.tsx`.
  </action>
  <verify>
    <automated>cd /Users/codysecret1/Desktop/IC-podcast && grep -q "DownloadView" app/download/page.tsx && ! grep -q "'use client'" app/download/page.tsx && ! grep -qi "placeholder" app/download/page.tsx && npm run build && npm run test:ci</automated>
  </verify>
  <acceptance_criteria>
    - `app/download/page.tsx` imports `DownloadView` from `@/components/DownloadView`.
    - `app/download/page.tsx` does not contain `'use client'`.
    - `app/download/page.tsx` does not contain the word `Placeholder`.
    - `npm run build` exits 0.
    - `npm run test:ci` exits 0 across the entire suite.
  </acceptance_criteria>
  <done>The /download route renders the full DownloadView; the post-session UI is reachable from RoomView's End Session button (wired in Plan 03).</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- `npm run test:ci` passes (DownloadView suite + all prior suites).
- Manual smoke: visit `/download` directly — see headline `Your sessions.`, four rows, and `+ New session` button. Click `+ New session` -> `/lobby`. After Plan 03 lands, from `/room` clicking `End session` navigates here.
</verification>

<success_criteria>
- DownloadView renders verbatim placeholder data sourced from the HTML prototype.
- Component accepts a `sessions` prop so Phase 4 can drop in real data without a rewrite (D-10).
- No file overlap with Plan 03 — both plans run safely in parallel in Wave 3.
</success_criteria>

<output>
Create `.planning/phases/01-ui-development/01-04-SUMMARY.md` when done. Include: placeholder session values used (confirm against HTML), how `sessions` prop maps to rendered rows, and notes on accessibility decisions for the `.dl` element.
</output>
