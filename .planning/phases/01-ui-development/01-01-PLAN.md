---
phase: 01-ui-development
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - jest.config.ts
  - jest.setup.ts
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/login/page.tsx
  - app/lobby/page.tsx
  - app/room/page.tsx
  - app/download/page.tsx
  - components/LoginView.tsx
  - __tests__/page.test.tsx
  - __tests__/LoginView.skeleton.test.tsx
autonomous: true
requirements: [UI-ROUTING, UI-LOGIN]
tags: [nextjs, scaffold, walking-skeleton, app-router]

must_haves:
  truths:
    - "Visiting / in a browser ends up at /login"
    - "The page at /login displays the headline 'Record together.' and a working email + password form"
    - "Submitting valid credentials on /login navigates the browser to /lobby"
    - "Topbar shows 'IC Podcast Recorder' on every page with no red dot"
    - "Inter font is applied to the body and design tokens from globals.css render the dark background"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16 + React 19 + TypeScript + Jest dependencies"
      contains: "\"next\": \"16"
    - path: "app/globals.css"
      provides: "All design tokens and base body styles ported from HTML"
      contains: "--accent:   #d9b96a"
    - path: "app/layout.tsx"
      provides: "Root layout with Inter font, frame, topbar, stage"
      contains: "next/font/google"
    - path: "app/page.tsx"
      provides: "Root redirect to /login"
      contains: "redirect('/login')"
    - path: "app/login/page.tsx"
      provides: "Login route shell"
      contains: "LoginView"
    - path: "components/LoginView.tsx"
      provides: "Minimal login form: email + password fields, validation, router.push to /lobby"
      contains: "router.push('/lobby')"
  key_links:
    - from: "app/layout.tsx"
      to: "next/font/google Inter"
      via: "className={inter.variable} on <html>"
      pattern: "inter\\.variable"
    - from: "app/page.tsx"
      to: "/login"
      via: "redirect() from next/navigation"
      pattern: "redirect\\(['\"]/login['\"]\\)"
    - from: "components/LoginView.tsx"
      to: "/lobby"
      via: "useRouter().push"
      pattern: "router\\.push\\(['\"]/lobby['\"]\\)"
---

## Phase Goal

**As a** podcast host, **I want to** open the app and reach a working login screen that takes me to the lobby, **so that** I can prove the project's UI scaffolding is real before any deeper UI is built.

<objective>
Stand up the Next.js project, port the HTML prototype's design tokens and topbar into `app/layout.tsx` + `app/globals.css`, create all four route shells (`/login`, `/lobby`, `/room`, `/download`) plus root redirect, and ship the thinnest possible LoginView that exercises the stack end-to-end: form state, client-side validation, and `router.push('/lobby')`.

Purpose: This is the Walking Skeleton. After this plan, the full stack (build, routing, fonts, CSS tokens, Server/Client component boundary, navigation) is proven working. Subsequent plans add UI surface on top of an already-running app.
Output: A runnable Next.js project with all routes registered, dark theme applied, topbar visible, and a working login → lobby navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-development/01-CONTEXT.md
@.planning/phases/01-ui-development/01-RESEARCH.md
@.planning/phases/01-ui-development/01-PATTERNS.md
@.planning/phases/01-ui-development/SKELETON.md
@IC Podcast Recorder.html

<interfaces>
Project structure target (RESEARCH "Recommended Project Structure"):

```
ic-podcast/  (= repo root, working directory)
├── app/
│   ├── globals.css
│   ├── layout.tsx
│   ├── page.tsx            redirect to /login
│   ├── login/page.tsx      thin shell -> <LoginView />
│   ├── lobby/page.tsx      thin shell, placeholder text in this plan
│   ├── room/page.tsx       thin shell, placeholder text in this plan
│   └── download/page.tsx   thin shell, placeholder text in this plan
├── components/
│   └── LoginView.tsx       'use client' minimal form
├── __tests__/
│   ├── page.test.tsx
│   └── LoginView.skeleton.test.tsx
├── eslint.config.mjs
├── jest.config.ts
├── jest.setup.ts
├── next.config.ts
├── tsconfig.json
└── package.json
```

Inter font integration (RESEARCH Pattern 2): import `Inter` from `next/font/google`, call with `{ subsets: ['latin'], display: 'swap', variable: '--font-inter' }`, apply `inter.variable` to `<html className>`. Then `--sans` token in globals.css references `var(--font-inter)`.

Page boundary rule (RESEARCH Pitfall 3): `app/**/page.tsx` and `app/layout.tsx` are Server Components — NO `'use client'` directive. `components/LoginView.tsx` IS a Client Component — first line `'use client'`.

Tailwind is the create-next-app default in Next.js 16 (RESEARCH Pitfall 5): MUST pass `--no-tailwind` when scaffolding. Confirm no `@import 'tailwindcss'` in globals.css.

Import alias: `@/*` -> repo root, configured in tsconfig.json by create-next-app `--import-alias "@/*"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Next.js project at repo root</name>
  <files>package.json, tsconfig.json, next.config.ts, eslint.config.mjs, app/globals.css, app/layout.tsx, app/page.tsx, .gitignore</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/phases/01-ui-development/01-RESEARCH.md (Standard Stack, Recommended Project Structure, Common Pitfalls #5, State of the Art)
    - .planning/phases/01-ui-development/SKELETON.md
    - IC Podcast Recorder.html (lines 1-460 — confirm style block + topbar markup)
  </read_first>
  <behavior>
    - `package.json` declares dependencies: `next` (16.x), `react` (19.x), `react-dom` (19.x), and devDependencies `typescript`, `@types/react`, `@types/node`, `eslint`, `eslint-config-next`.
    - `package.json` scripts include: `dev`, `build`, `start`, `lint`.
    - `npm run build` succeeds without errors.
    - `tsconfig.json` declares `paths: { "@/*": ["./*"] }`.
    - `app/globals.css` contains NO `@import 'tailwindcss'` and NO Tailwind directives.
    - No `tailwind.config.*` or `postcss.config.*` files exist.
    - No `src/` directory exists; `app/` is at repo root.
  </behavior>
  <action>
    Run `npx create-next-app@latest .` (or with a temp name, then move contents to repo root) with EXPLICIT flags to skip Tailwind and produce App Router + TypeScript + ESLint, no src directory, import alias `@/*`. Recommended invocation: `npx create-next-app@latest ic-podcast --typescript --no-tailwind --eslint --app --no-src-dir --import-alias "@/*"` then move contents into the repo root (`/Users/codysecret1/Desktop/IC-podcast`), preserving existing `.planning/`, `IC Podcast Recorder.html`, and `.git`. Do NOT pass `--yes`. After scaffolding, REPLACE the auto-generated `app/globals.css`, `app/layout.tsx`, and `app/page.tsx` in Task 2. For now, verify the scaffold output, remove any Tailwind artifacts that may have slipped in (any `tailwind.config.*`, `postcss.config.*`, `@import 'tailwindcss'` line in globals.css), and confirm scripts and dependency versions in `package.json` match RESEARCH Standard Stack. Update `.gitignore` if create-next-app overwrote it — preserve the existing entries for `.planning/` artifacts already tracked.
  </action>
  <verify>
    <automated>cd /Users/codysecret1/Desktop/IC-podcast && test -f package.json && test -f tsconfig.json && test -d app && test ! -f tailwind.config.ts && test ! -f tailwind.config.js && test ! -f postcss.config.js && test ! -f postcss.config.mjs && ! grep -q "tailwindcss" app/globals.css && grep -q '"next":' package.json && grep -q '"@/\*"' tsconfig.json && npm install && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `"next"` in dependencies whose value starts with `"16` or `"^16`.
    - `tsconfig.json` contains the literal string `"@/*"` in its `paths` block.
    - `app/globals.css` exists and `grep -c tailwindcss app/globals.css` returns 0.
    - No file named `tailwind.config.ts`, `tailwind.config.js`, `postcss.config.js`, or `postcss.config.mjs` exists at repo root.
    - `app/`, `app/page.tsx`, `app/layout.tsx` all exist (auto-generated content acceptable — Task 2 replaces them).
    - `npm install` exits 0 and `npm run build` exits 0.
    - `.planning/` directory is untouched (still contains `phases/`, `PROJECT.md`, `ROADMAP.md`, `STATE.md`).
    - `IC Podcast Recorder.html` still exists at repo root.
  </acceptance_criteria>
  <done>Next.js project is initialized at the repo root, dependencies install, build succeeds, no Tailwind config files or imports remain, `.planning/` and the HTML prototype are preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port design tokens, root layout, route shells, and Walking Skeleton LoginView</name>
  <files>app/globals.css, app/layout.tsx, app/page.tsx, app/login/page.tsx, app/lobby/page.tsx, app/room/page.tsx, app/download/page.tsx, components/LoginView.tsx, jest.config.ts, jest.setup.ts, __tests__/page.test.tsx, __tests__/LoginView.skeleton.test.tsx, package.json</files>
  <read_first>
    - IC Podcast Recorder.html (FULL FILE — lines 1-977; needed for verbatim CSS port and topbar/login markup)
    - .planning/phases/01-ui-development/01-CONTEXT.md (D-01 through D-12)
    - .planning/phases/01-ui-development/01-RESEARCH.md (Patterns 1-5, Code Examples, Common Pitfalls)
    - .planning/phases/01-ui-development/01-PATTERNS.md (`app/globals.css`, `app/layout.tsx`, `app/page.tsx`, thin shells, `components/LoginView.tsx` sections)
    - .planning/phases/01-ui-development/SKELETON.md
    - app/globals.css (current scaffold-generated file — to be overwritten)
    - app/layout.tsx (current scaffold-generated file — to be overwritten)
    - app/page.tsx (current scaffold-generated file — to be overwritten)
    - package.json (to add Jest dev dependencies and `test` script)
  </read_first>
  <behavior>
    - GET `/` returns 307 with `Location: /login` (Next.js server-side redirect from `redirect()`).
    - GET `/login` returns HTML containing the strings `IC Podcast Recorder`, `Record`, and `together` (with `<i>together.</i>` wrapping the accent word).
    - GET `/lobby`, `/room`, `/download` return HTML status 200 with at least a placeholder heading identifying the route (e.g. `Lobby`, `Room`, `Download`) — full content arrives in Plans 02-04.
    - Topbar markup is present on every page: `<header class="topbar">` with brand text `IC Podcast Recorder`. No `<span class="dot">` element (D-06).
    - In `app/layout.tsx`, the topbar's `topbar-right` block exists but is hidden (`style={{ display: 'none' }}`) in Phase 1 (D-05).
    - `app/globals.css` contains the verbatim token block from HTML lines 11-23 with one substitution: `--sans` value uses `var(--font-inter)` instead of `"Inter"` (D-03).
    - `app/globals.css` contains the verbatim body reset, `.frame`, `.topbar`, `.brand`, `.topbar-right`, and `.stage` rules from HTML lines 26-79 (with `.brand .dot` rule OMITTED per D-06, and `.view`/`.view.active` rules OMITTED since Next.js routing replaces them).
    - `app/globals.css` contains the verbatim CSS for `.login-wrap`, `.login-inner`, `.mark`, `.mark i`, `.tag`, `.tabs`, `.tab`, `.field`, `.btn` (and `.btn.primary`, `.btn.ghost`, `.btn.block`), `.submit-row`, `.error`, `.login-alt` so the Walking Skeleton login form is styled.
    - `app/globals.css` ALSO contains every remaining rule from HTML lines 82-451 ported verbatim (lobby, room, panels, pillar, rec-btn, countdown, post-session, all keyframes) so Plans 02-04 do not need to re-touch globals.css. The ONLY edits are: `--sans` fix, removal of `.brand .dot` rule, removal of `.view`/`.view.active` rules.
    - `app/layout.tsx` imports `Inter` from `next/font/google`, configures it with `subsets: ['latin']`, `display: 'swap'`, `variable: '--font-inter'`, and applies `inter.variable` as the `<html>` className. It imports `./globals.css`. It is a Server Component (NO `'use client'`).
    - `app/page.tsx` is a Server Component that calls `redirect('/login')` from `next/navigation` and returns nothing meaningful.
    - `app/login/page.tsx`, `app/lobby/page.tsx`, `app/room/page.tsx`, `app/download/page.tsx` are Server Components. Each is a thin shell. `app/login/page.tsx` renders `<LoginView />`. The other three render a placeholder `<div>` with text identifying the route (`Lobby`, `Room`, `Download`) wrapped in something neutral — these placeholders are replaced in Plans 02-04.
    - `components/LoginView.tsx` is a Client Component (`'use client'` is the literal first line). It renders the login-tab markup from HTML lines 472-498 (the `.login-wrap > .login-inner > .mark > "Record <i>together.</i>"`, `.tag`, `.tabs`, and the login-only form with email + password fields, a `.submit-row` containing a `Sign in` button, and an `.error` div).
    - The Register tab and register form are NOT implemented in this plan (Plan 02 adds them). The tabs row may render both tabs visually but the `Create account` tab can be a non-functional placeholder for now (it does NOT need to switch forms in this plan).
    - On submit, the form: prevents default, reads `email` and `password` from the form, validates that both are non-empty and `email` includes `@` and `password.length >= 10`. If validation fails, sets local error state rendered into the `.error` div. If validation passes, calls `useRouter().push('/lobby')`.
    - `jest.config.ts` is configured for Next.js + React using `next/jest` (or equivalent), with `testEnvironment: 'jsdom'`, `setupFilesAfterEach` (or `setupFilesAfterEach`/`setupFiles`) pointing at `jest.setup.ts`, and module name mapper supporting the `@/` alias.
    - `jest.setup.ts` imports `@testing-library/jest-dom`.
    - `package.json` adds devDependencies: `jest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jest-environment-jsdom`, `@types/jest`, `ts-node` (if needed for `jest.config.ts`). A `test` script runs `jest`.
    - `__tests__/page.test.tsx` renders the root `app/page.tsx` and asserts that `redirect` from `next/navigation` was invoked with `/login` (use `jest.mock('next/navigation', ...)`).
    - `__tests__/LoginView.skeleton.test.tsx` renders `<LoginView />`, asserts the headline text `Record` and `together` appear, finds the email + password inputs by label, asserts that submitting with an empty form shows an error message, and asserts that submitting with a valid email + 10+ char password invokes `useRouter().push` with `/lobby` (mock `useRouter`).
  </behavior>
  <action>
    Replace the scaffold-generated `app/globals.css` with a verbatim port of the HTML `<style>` block (HTML lines 10-452), making EXACTLY these three edits: (1) the `--sans` token value becomes `var(--font-inter), -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`; (2) delete the `.brand .dot { ... }` rule entirely; (3) delete the `.view { ... }` and `.view.active { ... }` rules. Do not introduce any other modifications, comments, or reordering.

    Replace `app/layout.tsx` with the layout pattern from PATTERNS.md `app/layout.tsx` section: import Inter with `variable: '--font-inter'`, set `<html lang="en" className={inter.variable}>`, render `<body>` containing `<div class="frame">` -> `<header class="topbar">` (brand text only, no `.dot` span) and the `<div class="topbar-right" style={{ display: 'none' }}>` placeholder, plus `<main class="stage">{children}</main>`. Export typed RootLayout. NO `'use client'`.

    Replace `app/page.tsx` with a Server Component that imports `redirect` from `next/navigation` and calls `redirect('/login')` inside the default export.

    Create `app/login/page.tsx` as a Server Component that imports the default export `LoginView` from `@/components/LoginView` and renders `<LoginView />`. Create `app/lobby/page.tsx`, `app/room/page.tsx`, `app/download/page.tsx` as Server Components that render a placeholder `<div>` with a single `<h1>` containing the route name (`Lobby`, `Room`, `Download`) — these are explicit placeholders to be replaced in later plans; mark each with a `{/* Placeholder — replaced in Plan 0N */}` comment.

    Create `components/LoginView.tsx` as a Client Component (`'use client'` first line). Follow the PATTERNS.md `components/LoginView.tsx` section but include ONLY the login tab body (skip the register form for this plan). Required structure: `<div class="login-wrap"><div class="login-inner">` containing the `.mark` headline with `<i>together.</i>`, the `.tag` subtitle, the `.tabs` row (both tab elements visible, only `Sign in` clickable / styled with `.on`), and a `<form>` for the login tab with email field, password field, an `.error` div, and a `.submit-row` containing a `<button class="btn primary block">Sign in</button>`. On submit: `e.preventDefault()`, read fields via `e.currentTarget.elements`, run the three validation checks (non-empty, `@` in email, password length >= 10), set error state and bail on failure, otherwise call `router.push('/lobby')` using `useRouter` from `next/navigation`.

    Add Jest configuration: install `jest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jest-environment-jsdom`, `@types/jest` as devDependencies. Create `jest.config.ts` using `next/jest` preset (`import nextJest from 'next/jest.js'`) configured with `testEnvironment: 'jsdom'`, `setupFilesAfterEach: ['<rootDir>/jest.setup.ts']`, and the `@/` alias mapped via `moduleNameMapper: { '^@/(.*)$': '<rootDir>/$1' }`. Create `jest.setup.ts` that imports `@testing-library/jest-dom`. Add `"test": "jest"` and `"test:ci": "jest --watchAll=false"` to `package.json` scripts.

    Create `__tests__/page.test.tsx`: mock `next/navigation` (`redirect: jest.fn()`), import the default export of `app/page.tsx`, invoke it, and assert `redirect` was called with `/login`.

    Create `__tests__/LoginView.skeleton.test.tsx`: mock `next/navigation` (`useRouter: () => ({ push: jest.fn() })`), render `<LoginView />`, query the headline (`getByText` matching `/Record/i` and `/together/i`), query inputs by label/role, simulate submit with empty inputs and assert an error message containing `fill` is shown, simulate submit with `test@example.com` + `0123456789` and assert the mocked `push` was called with `/lobby`. Capture the mocked router from a shared `pushMock` so the assertion can verify the argument.
  </action>
  <verify>
    <automated>cd /Users/codysecret1/Desktop/IC-podcast && grep -c "redirect('/login')" app/page.tsx | grep -v '^0$' && grep -q "next/font/google" app/layout.tsx && grep -q "var(--font-inter)" app/globals.css && grep -q "#d9b96a" app/globals.css && grep -q "IC Podcast Recorder" app/layout.tsx && ! grep -q "class=\"dot\"" app/layout.tsx && ! grep -q "className=\"dot\"" app/layout.tsx && grep -q "'use client'" components/LoginView.tsx && grep -q "router.push('/lobby')" components/LoginView.tsx && npm run build && npm run test:ci</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@import 'tailwindcss'" app/globals.css` returns 0.
    - `grep -c "#d9b96a" app/globals.css` returns at least 1 (accent token present).
    - `grep -c "var(--font-inter)" app/globals.css` returns at least 1.
    - `grep -c "\\.brand \\.dot" app/globals.css` returns 0 (rule removed per D-06).
    - `grep -c "\\.view\\.active" app/globals.css` returns 0.
    - `app/layout.tsx` contains `next/font/google` and `inter.variable` and the string `IC Podcast Recorder`.
    - `app/layout.tsx` does NOT contain `'use client'` as a directive.
    - `app/page.tsx` contains `redirect('/login')` and imports from `next/navigation`.
    - `app/login/page.tsx` imports `LoginView` and renders `<LoginView />`.
    - `app/lobby/page.tsx`, `app/room/page.tsx`, `app/download/page.tsx` each exist and `grep -l Placeholder app/lobby/page.tsx app/room/page.tsx app/download/page.tsx` lists all three.
    - First non-empty, non-comment line of `components/LoginView.tsx` is `'use client'`.
    - `components/LoginView.tsx` contains the literal string `router.push('/lobby')` and contains `<i>together.</i>` (or React equivalent `<i>together.</i>`).
    - `jest.config.ts`, `jest.setup.ts`, `__tests__/page.test.tsx`, `__tests__/LoginView.skeleton.test.tsx` all exist.
    - `package.json` scripts include `test` and `test:ci`.
    - `npm run build` exits 0.
    - `npm run test:ci` exits 0 with both test files passing.
    - Behavior: starting `npm run dev` and issuing `curl -sI http://localhost:3000/` returns `HTTP/1.1 307` and `location: /login` (manual or scripted dev-server check is acceptable; not required for the automated gate above).
  </acceptance_criteria>
  <done>The dark UI loads on every route; `/` redirects to `/login`; the login page shows the styled headline and form; submitting valid credentials navigates to `/lobby` placeholder; tests prove the redirect and the login submit behavior.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- `npm run test:ci` passes both included tests.
- Manual smoke: `npm run dev`, then visiting `http://localhost:3000/` redirects to `/login`; the dark theme + topbar render; submitting `test@example.com` + `0123456789` navigates to `/lobby`.
</verification>

<success_criteria>
- The Walking Skeleton interaction described in `SKELETON.md` works end-to-end.
- All four route shells exist so subsequent plans add UI without touching scaffolding.
- `app/globals.css` is the full, final port of the HTML stylesheet (with the three required edits) — Plans 02-04 do not need to edit it.
- `components/LoginView.tsx` exists as a minimal vertical slice (form + validation + navigation). Plan 02 extends it with the register tab and the UI primitives.
</success_criteria>

<output>
Create `.planning/phases/01-ui-development/01-01-SUMMARY.md` when done. Include: scaffold flags used, list of files created/replaced, what was ported verbatim from the HTML vs modified, jest configuration approach, and confirmation of the Walking Skeleton interaction.
</output>
